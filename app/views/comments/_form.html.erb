<%= form_for(:comment, as: post, url: post_comments_path(post), action: "create") do |f| %>
  <div class="form-group">
    <%= f.text_field :body, rows: 8, class: 'form-control', placeholder: "Enter comment" %>
  </div>
  <%= f.submit "Save", class: 'btn btn-success' %>
<% end %>