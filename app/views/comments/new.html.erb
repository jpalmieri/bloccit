<h1>New Comment</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for comments:</p>
    <ul>
      <li>Make sure the comment is appropriate.</li>
      <li>Never insult dogs.</li>
      <li>Smile when you type.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@topic, @post, @post.comments.new] do |f| %>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_field :body, rows: 8, class: 'form-control', placeholder: "Enter comment" %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>